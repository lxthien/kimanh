<aside class="col-lg-3 d-none hidden-xs hidden-sm" id="sidebar">
    <div class="sidebar">
        {{ render(controller('AppBundle:News:caculatorCostConstruction', {'type': 'sidebar'})) }}
        {% if (post is not defined or post is empty or post.getTemplate() != '1_column') and category is defined and category is not empty and category.getId() != 1 %}
            {{ include('news/prices.html.twig') }}
            {% if category.getShowPostRelated() %}{{ render(controller('AppBundle:News:listNewsByCategorySidebar', {'categoryId': category.getId(), 'title': 'Tin cùng chuyên mục', 'postNumber': 25})) }}{% endif %}
            {{ render(controller('AppBundle:News:listNewsByCategorySidebar', {'categoryId': 2, 'title': 'Các mẫu nhà đẹp'})) }}
            {{ render(controller('AppBundle:News:listNewsByCategorySidebar', {'categoryId': 8, 'title': 'Dự án tiêu biểu'})) }}
        {% endif %}
    </div>
</aside>
