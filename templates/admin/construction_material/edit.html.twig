{% extends 'admin/layout.html.twig' %}

{% block main %}
    <div class="content mt-3">
        <div class="animated fadeIn">
            <div class="row">
                <div class="col-lg-12">
                    <div class="card">
                        <div class="card-header">
                            <strong class="card-title">Cập nhật vật tư #{{ object.id }}</strong>
                        </div>
                        <div class="card-body">
                            {{ include('admin/construction_material/_form.html.twig', {
                                form: form,
                                button_label: 'Lưu thay đổi'
                            }, with_context = false) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
